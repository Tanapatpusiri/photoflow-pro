export const metadata = { title: 'PhotoFlow Pro', description: 'AI-assisted photography workflow for photographers and parents' };
export default function RootLayout({ children }) { return <html lang="th"><body>{children}</body></html>; }
